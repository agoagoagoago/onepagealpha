/**
 * Site-wide defaults you may want to edit in one place.
 */

// TODO: Replace this with the final Buy Me a Coffee URL.
// Individual companies can override this via `buyMeACoffeeUrl` in data/companies.ts.
export const BUY_ME_A_COFFEE_URL = "https://buymeacoffee.com/onepagealpha";

// Site logo (wide ~3:1 lockup). File lives in public/brand/.
// To swap the logo, replace that file; if the extension changes, update this path.
export const LOGO_PATH = "/brand/Logo_OnePage_Alpha.png";
